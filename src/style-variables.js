const styleObjects = [];
const primaryColor = "#4e7494";

// Globals

styleObjects.push({
    "font-size": "16px"
});

// Button

styleObjects.push({
    "button-color": primaryColor
});

module.exports = Object.assign({}, ...styleObjects);